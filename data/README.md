# Data directory

- `inputs/` holds local input files used by scripts. These are intentionally kept out of Git.
- `outputs/` contains generated artifacts from crawling, analysis, and exports.

This folder is for local, reproducible data only. Use scripts to regenerate outputs as needed.
